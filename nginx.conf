server {
  listen 80;
  server_name localhost;

  location / {
    root /usr/share/nginx/html; # Шлях до папки із зібраними статичними файлами
    index index.html index.htm;
    try_files $uri $uri/ / index.html =404;
  }
}